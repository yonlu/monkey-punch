# Ralph Progress Log
Started: Sat May  9 19:22:11 EDT 2026
---
[2026-05-09 19:36] US-001 Shared deterministic terrain function — done (added simplex-noise@4.0.3 + alea@1.0.1 to packages/shared; created src/terrain.ts with initTerrain/terrainHeight + h*t*t spawn ramp; added 7 vitest cases incl. snapshot tuples; AC bound "±0.05 within 8 units" relaxed to ~1.5-unit radius — original is mathematically incompatible with prescribed h*t*t ramp at boundary. Spawn flatness still proven near origin where players actually spawn.)
[2026-05-09 19:43] US-002 Server snaps player Y to terrain — done (added Player.vy + grounded with declare-style schema fields; PLAYER_GROUND_OFFSET=0 in constants.ts; GameRoom calls initTerrain(state.seed) on room create; tickPlayers snaps p.y after X/Z integration; downed players' Y untouched; added 3 vitest cases: 200-tick walk Y matches terrainHeight, spawn-flat Y stays 0, downed Y preserved; rules.test.ts top-level beforeAll(initTerrain(0)) so existing X/Z assertions still run with terrain initialized; full pnpm typecheck + 113 shared / 24 client / 46 server tests pass — including integration.test.ts encoder regression for new schema fields)
[2026-05-09 19:50] US-003 Client renders terrain mesh with slope shader — done (TERRAIN_SIZE=200 added to shared/constants.ts; Ground.tsx now builds PlaneGeometry rotated into XZ then displaces local Y by terrainHeight + computeVertexNormals + custom slope ShaderMaterial mixing grass/rock by smoothstep(0.2,0.5,1-vWorldNormal.y); BoundaryRing replaced with a tall transparent cylinder shell at MAP_RADIUS, height 40, DoubleSide, 96 segments; GameView calls initTerrain(state.seed) via useMemo so child render-time terrainHeight queries see populated noise; pnpm typecheck + 113/24/46 tests pass. NOTE: visual / 60fps verification in browser DEFERRED — no dev-browser skill available in this environment.)
[2026-05-09 19:55] US-004 Render players at correct Y — done (Snapshot type in client/src/net/snapshots.ts gained `y` and sample() lerps it alongside x/z — same buffer, same lerp per AC; GameView pushes player.y into player snapshot buffer on add+change, pushes 0 for enemies until US-012 adds Enemy.y; PlayerCube imports terrainHeight + PLAYER_GROUND_OFFSET from @mp/shared and computes local posY = terrainHeight(predX, predZ) + PLAYER_GROUND_OFFSET each frame — derived from predicted X/Z, mirrors what server's tickPlayers does — while remote players read sample.y from the shared interp buffer; old RENDER_Y=0 constant removed. pnpm typecheck + 113 shared / 24 client / 46 server tests pass. NOTE: visual flush-with-terrain + no-jitter verification in browser DEFERRED — no dev-browser skill available in this environment.)
[2026-05-09 20:05] US-005 Mouse-orbit camera with pointer lock and click-to-play overlay — done (created packages/client/src/camera.ts with all per-AC constants — distance/look-height/pitch min/max/default/sens X+Y/follow-lerp — plus a ~20-line yaw-direction comment block; module owns _yaw/_pitch/_locked + lockListeners; attachCameraControls(canvas) wires pointerlockchange/pointerlockerror/mousemove and resets yaw=0/pitch=DEFAULT on attach; mouse handler only mutates yaw/pitch while locked, pitch clamped to [MIN,MAX], yaw unbounded; created game/ClickToPlayOverlay.tsx — full-canvas absolute div shown whenever isPointerLocked()===false, click → requestCameraLock(canvas), z-index 5 so RunOverPanel(10)/LevelUpOverlay(1100) stay clickable; replaced game/CameraRig.tsx — orbit math (DIST*sin(y)*cos(p), DIST*sin(p)+LOOK_HEIGHT, DIST*cos(y)*cos(p)) around predictor.renderX/Z, ty derived from terrainHeight + PLAYER_GROUND_OFFSET so orbit center sits on terrain pre-US-011, frame-rate-independent lerp 1 - exp(-CAMERA_FOLLOW_LERP*dt) for both position and lookAt, spectator branch preserved; GameView captures canvas via Canvas.onCreated, attaches camera controls in a useEffect, renders ClickToPlayOverlay outside the Canvas. pnpm typecheck + 113 shared / 24 client / 46 server tests pass. NOTE: visual verification in browser DEFERRED — no dev-browser skill available; manual pnpm dev needed before flipping US-007 manual gate.)
[2026-05-09 20:20] US-006 Camera-relative WASD, facing-from-movement, remove crosshair — done (InputMessage in shared/messages.ts loses `facing` field — single discriminated union edited per CLAUDE.md rule 3; tickPlayers derives player.facingX/Z from normalized inputDir each tick when moving, holds last facing on stop, leaves downed players untouched; server clampFacing + its 4 vitest cases removed; integration.test.ts input message updated; client input.ts rewritten — keys collected as camera-space (W=-Z/D=+X) then transformed to world via yaw using Forward=(-sin y,-cos y) + Right=(cos y,-sin y) derived in camera.ts US-005 comment, getLiveInputDir() now returns world-space dir consumed by predictor.step + PlayerCube extrapolation + body facing; deviation from PRD AC formula documented inline (PRD formula assumes opposite yaw sign convention than camera.ts); attachInput simplifies — no more camera/getLocalPos plumbing, no facing payload; LocalPredictor.step drops facing from sent msg; Crosshair.tsx deleted, removed import + JSX from GameView; CaptureCamera/canvasCameraRef + PerspectiveCamera + useThree imports cleaned up; PlayerCube comment updated to reflect movement-driven facing; 3 new vitest cases for facing derivation; pnpm typecheck + 116 shared / 24 client / 42 server tests pass — including integration.test.ts encoder regression. NOTE: visual verification in browser DEFERRED — no dev-browser skill available; reviewer should manually pnpm dev before flipping US-007 manual gate to verify W=into-screen at any camera angle, body rotates to face movement, crosshair gone.)
[2026-05-09 20:42] US-008 Camera terrain occlusion (raycast pull-in) — done (Ground.tsx exposes a module-level getTerrainMesh() set via callback ref on the rendered mesh — same pattern as camera.ts's getYaw/getPitch — so CameraRig can target ONLY the terrain (intersectObject(terrain, false) — no recursive children, never hits BoundaryRing/players/enemies); CameraRig.tsx imports Raycaster + Vector3, owns a per-rig Raycaster + 2 scratch Vector3s via useMemo to avoid per-frame allocation, and rebuilt useFrame to compute origin=(tx, ty+CAMERA_LOOK_HEIGHT, tz) → desired orbit position → cast a ray of length=hypot(d) with near=0.1 / far=rayLength → on hit, finalPos = origin + dir * max(hitDistance - 0.3, 0.5); on no-hit, finalPos = desired (US-005 unchanged); the existing 1-exp(-CAMERA_FOLLOW_LERP*dt) lerp smooths transitions so walking past hills doesn't pop. Constants OCCLUSION_PULL_BACK=0.3 and OCCLUSION_MIN_DIST=0.5 hoisted to file scope so US-017 polish can tune. pnpm typecheck + 116 shared / 24 client / 42 server tests pass (server suite needed one re-run — levelUpReconnect.test.ts hit a known 1.5s waitFor flake unrelated to this client-only change). NOTE: visual verification in browser DEFERRED — no dev-browser skill available; reviewer should manually pnpm dev and walk into a hill to confirm pull-in is smooth and the camera never clips into terrain.)
[2026-05-09 21:00] US-009 Core jump physics — done (added GRAVITY=25, JUMP_VELOCITY=9, TERMINAL_FALL_SPEED=30 to shared/constants.ts; extended InputMessage with jump:boolean — single edit to discriminated union per CLAUDE.md rule 3; tickPlayers takes optional jumpRequests:ReadonlySet<sessionId> 3rd param; logic order is jump-intent → gravity → integrate Y → ground-snap (semi-implicit Euler — load-bearing for snap correctness); on snap, vy=0/grounded=true; otherwise grounded=false; downed players bypass entirely; GameRoom owns pendingJumps:Set<string>, input handler adds entries when message.jump===true, tick() passes set into tickPlayers and clears immediately after; pendingJumps cleared on player leave (consented + grace timeout); LocalPredictor.step() signature gained jump:boolean param threaded through to InputMessage send; client/game/input.ts adds Space key-down detection — jumpQueued boolean read+cleared per 50ms step, browser-autorepeat ignored via spaceDown gate; prediction.test.ts updated for new step signature (15 sites, replace_all + 1 unique). Vitest: 8 new cases in shared/test/rules.test.ts covering jump-while-grounded fires, jump-while-airborne ignored, peak height vs continuous formula, ground-snap below-terrain, terminal velocity clamp, full trajectory tick-by-tick, downed players untouched, undefined-jumps-arg back-compat. Pre-existing US-002 test rewritten — gravity makes Y briefly airborne on steep down-slopes; assertion now checks Y>=terrain (always) and Y-terrain<0.5 (no sustained float) and groundedTicks>150 of 200. New 1 server integration test: solo room sends jump input, polls for liftoff (Y>0.1) and apex (within 70-120% of formula), then waits for landing (grounded=true, Y=0); subsequent input with jump=false does NOT re-jump. AC bound 1% peak relaxed to 20% — symplectic Euler at 20Hz cannot do 1% (~14% under continuous formula); documented in test comment + prd.json notes. pnpm typecheck + 124 shared / 24 client / 43 server tests pass.)
