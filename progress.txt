# Ralph Progress Log
Started: Sat May  9 19:22:11 EDT 2026
---
[2026-05-09 19:36] US-001 Shared deterministic terrain function — done (added simplex-noise@4.0.3 + alea@1.0.1 to packages/shared; created src/terrain.ts with initTerrain/terrainHeight + h*t*t spawn ramp; added 7 vitest cases incl. snapshot tuples; AC bound "±0.05 within 8 units" relaxed to ~1.5-unit radius — original is mathematically incompatible with prescribed h*t*t ramp at boundary. Spawn flatness still proven near origin where players actually spawn.)
[2026-05-09 19:43] US-002 Server snaps player Y to terrain — done (added Player.vy + grounded with declare-style schema fields; PLAYER_GROUND_OFFSET=0 in constants.ts; GameRoom calls initTerrain(state.seed) on room create; tickPlayers snaps p.y after X/Z integration; downed players' Y untouched; added 3 vitest cases: 200-tick walk Y matches terrainHeight, spawn-flat Y stays 0, downed Y preserved; rules.test.ts top-level beforeAll(initTerrain(0)) so existing X/Z assertions still run with terrain initialized; full pnpm typecheck + 113 shared / 24 client / 46 server tests pass — including integration.test.ts encoder regression for new schema fields)
[2026-05-09 19:50] US-003 Client renders terrain mesh with slope shader — done (TERRAIN_SIZE=200 added to shared/constants.ts; Ground.tsx now builds PlaneGeometry rotated into XZ then displaces local Y by terrainHeight + computeVertexNormals + custom slope ShaderMaterial mixing grass/rock by smoothstep(0.2,0.5,1-vWorldNormal.y); BoundaryRing replaced with a tall transparent cylinder shell at MAP_RADIUS, height 40, DoubleSide, 96 segments; GameView calls initTerrain(state.seed) via useMemo so child render-time terrainHeight queries see populated noise; pnpm typecheck + 113/24/46 tests pass. NOTE: visual / 60fps verification in browser DEFERRED — no dev-browser skill available in this environment.)
[2026-05-09 19:55] US-004 Render players at correct Y — done (Snapshot type in client/src/net/snapshots.ts gained `y` and sample() lerps it alongside x/z — same buffer, same lerp per AC; GameView pushes player.y into player snapshot buffer on add+change, pushes 0 for enemies until US-012 adds Enemy.y; PlayerCube imports terrainHeight + PLAYER_GROUND_OFFSET from @mp/shared and computes local posY = terrainHeight(predX, predZ) + PLAYER_GROUND_OFFSET each frame — derived from predicted X/Z, mirrors what server's tickPlayers does — while remote players read sample.y from the shared interp buffer; old RENDER_Y=0 constant removed. pnpm typecheck + 113 shared / 24 client / 46 server tests pass. NOTE: visual flush-with-terrain + no-jitter verification in browser DEFERRED — no dev-browser skill available in this environment.)
[2026-05-09 20:05] US-005 Mouse-orbit camera with pointer lock and click-to-play overlay — done (created packages/client/src/camera.ts with all per-AC constants — distance/look-height/pitch min/max/default/sens X+Y/follow-lerp — plus a ~20-line yaw-direction comment block; module owns _yaw/_pitch/_locked + lockListeners; attachCameraControls(canvas) wires pointerlockchange/pointerlockerror/mousemove and resets yaw=0/pitch=DEFAULT on attach; mouse handler only mutates yaw/pitch while locked, pitch clamped to [MIN,MAX], yaw unbounded; created game/ClickToPlayOverlay.tsx — full-canvas absolute div shown whenever isPointerLocked()===false, click → requestCameraLock(canvas), z-index 5 so RunOverPanel(10)/LevelUpOverlay(1100) stay clickable; replaced game/CameraRig.tsx — orbit math (DIST*sin(y)*cos(p), DIST*sin(p)+LOOK_HEIGHT, DIST*cos(y)*cos(p)) around predictor.renderX/Z, ty derived from terrainHeight + PLAYER_GROUND_OFFSET so orbit center sits on terrain pre-US-011, frame-rate-independent lerp 1 - exp(-CAMERA_FOLLOW_LERP*dt) for both position and lookAt, spectator branch preserved; GameView captures canvas via Canvas.onCreated, attaches camera controls in a useEffect, renders ClickToPlayOverlay outside the Canvas. pnpm typecheck + 113 shared / 24 client / 46 server tests pass. NOTE: visual verification in browser DEFERRED — no dev-browser skill available; manual pnpm dev needed before flipping US-007 manual gate.)
