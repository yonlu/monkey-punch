# Ralph Progress Log
Started: Sat May  9 22:30:00 EDT 2026
---
[2026-05-09 23:50] US-001 Architecture review checkpoint — done. Wrote docs/superpowers/specs/2026-05-09-m8-weapon-variety-design.md (~14 sections) covering: 5-arm WeaponDef union with per-behavior *Level types (matches M5 pattern, not a generic perLevel); full 8-weapon table with L1–L5 levels (Bolt + Orbit unchanged; 6 new); schema additions (Enemy.slowMultiplier+slowExpiresAt, BloodPool class, RoomState.bloodPools — all declare-style + defineTypes per landmine #1); 3 new combat events (melee_swipe, boomerang_thrown) + extending HitEvent with tag/weaponKind (instead of introducing damage_dealt — codebase already treats HitEvent as the canonical "enemy was damaged" event after M7 US-013); FireEvent gains weaponLevel + lockedTargetId. Tick-order amendment: tickStatusEffects before tickEnemies (so movement uses fresh slow), tickBloodPools after tickProjectiles + before tickGems (so this-tick pool kills drop gems before pickup checks). Neither new tick calls state.rng — seed schedule does NOT fork. Open-question resolutions: A1 boomerang freezes return target on owner downing; A2 stronger slow wins (ignore weaker-but-longer); A3 target locked at fire, no re-acquire — adds lockedTargetId on FireEvent so client homing is closed-form deterministic; A4 aura uses 3D distance (consistent with M7 projectile hit detection). Two CLAUDE.md edits queued for US-009/US-011 to land: rule 11 prose update + status-effect refactor note (>2 effect kinds → ArraySchema<StatusEffect>). Approved by Luke.
[2026-05-10 00:50] US-004 Ahlspiess — piercing line projectile, facing direction, infinite pierce — done. weapons.ts: ProjectileMesh enum gains 'spear'; Ahlspiess added at index 3 (Bolt=0, Orbit=1, Gakkung=2, Ahlspiess=3) with behavior { targeting:'facing', homingTurnRate:0, mesh:'spear' } and 5-level array — pierceCount:-1 at every level (infinite — only lifetime expiry despawns), hitCooldownPerEnemyMs:200 across all levels (gates same-enemy double-hit via the per-projectile Map US-002 set up), hitRadius growing 0.50→0.55→0.60→0.70→0.80 (the per-level visual-scale AC), damage 25→54, cooldown 1.00→0.80. ProjectileSwarm.tsx restructured — third InstancedMesh added (cylinderGeometry 0.04x0.04x1.2 gold #e8c460, distinct from Gakkung's shorter brown 0.05x0.05x0.6 and Bolt's 0.15 sphere); ALL three arms now use matrix.compose for consistency (sphere reads a module-scope IDENTITY_QUAT to avoid per-frame allocation; elongated and spear use Y_AXIS→dir slerp via Quaternion.setFromUnitVectors); per-instance scale = stats.hitRadius / 0.4 baseline so Ahlspiess visibly grows L1→L5 (1.25→2.0×) while Bolt + Gakkung stay flat (constant hitRadius 0.4 → scale 1.0). Tests: 5 in weapons.test.ts (kind index 3, behavior shape facing+0+spear, infinite pierce at all 5 levels, hitCooldownPerEnemyMs > 0 across all levels, strictly-growing hitRadius + damage); 2 in rules.test.ts (Ahlspiess fires along player.facing with lockedTargetId=-1 + pierceRemaining=-1 + hitCooldownPerEnemyMs=200 + homingTurnRate=0 baked correctly; facing-mode still gates on any-enemy-in-range so an empty field doesn't waste shots — AD10 cooldown clamps at 0). NO name-based branching (rule 12 grep clean). pnpm typecheck + 182 shared (was 175) / 34 client / 43 server tests pass.
[2026-05-10 00:35] US-003 Gakkung Bow — long-range homing projectile, furthest targeting — done. weapons.ts: new ProjectileMesh enum ('sphere' | 'elongated') for per-weapon visual distinction (rule 12-compliant — enum dispatch not name dispatch); projectile behavior gains `mesh` field; Bolt updated to mesh:'sphere' (visual unchanged); Gakkung Bow added at index 2 per design doc (Bolt=0, Orbit=1, Gakkung=2 — order matters: WEAPON_KINDS index is the wire identifier so reordering would break a M5/M6/M7 test if it existed) with targeting:'furthest', homingTurnRate:Math.PI*0.8 (~144°/s — mild homing, can still miss fast targets), mesh:'elongated', and 5-level array (damage 18→22→26→30→36, cooldown 0.85→0.65, pierceCount stepping 1/1/2/2/3 — pierce growth gates Gakkung's role as "the pierce weapon" past L3). Client ProjectileSwarm.tsx rewrite: two parallel InstancedMeshes — existing yellow sphere kept for Bolt; new elongated cylinder (CylinderGeometry 0.05x0.05x0.6) brown (#a86b3c) emissive; per-frame loop dispatches by `def.behavior.mesh`, sphere mesh uses translation matrix (rotationally invariant), elongated mesh uses compose(position, Quaternion.setFromUnitVectors(Y_AXIS, dirVec), unitScale) so the cylinder points along its motion direction; lifted the M5 weaponLevel hardcoded `statsAt(def, 1)` to `statsAt(def, fe.weaponLevel)` — visual stats can now scale per level (Ahlspiess hitRadius growth in US-004 will read this). KNOWN APPROXIMATION documented inline at top of ProjectileSwarm: client renders straight-line motion from FireEvent payload even for homing weapons; server simulates real curved homing internally and emits `hit` at the curved-impact moment, so functional outcomes (hit timing, damage placement) match — only the mid-flight visual position diverges. Mild for Gakkung (gentle homing, 1.2s lifetime, slow targets); revisit in polish pass if playtest reveals. Tests: 4 new in weapons.test.ts (kind index 2 + furthest/elongated/Math.PI*0.8 behavior fields, pierceCount L1/L3/L5 progression, damage strictly increasing per level, flight range > TARGETING_MAX_RANGE sanity); 1 new in rules.test.ts (Gakkung end-to-end: 3 enemies at distance 5/12/18, tickWeapons selects id=3 furthest, projectile pushed with homingTurnRate baked + lockedTargetId=3, FireEvent emits weaponKind=2/weaponLevel=1/lockedTargetId=3). NO name-based branching anywhere (rule 12 verified by grep — only hit is the literal 'Gakkung Bow' string in the WEAPON_KINDS data table). pnpm typecheck + 175 shared (was 170) / 34 client / 43 server tests pass.
[2026-05-10 00:18] US-002 WeaponBehavior refactor — extended projectile params, Bolt non-regression — done. weapons.ts: TargetingMode = 'nearest'|'furthest'|'facing'; ProjectileLevel adds pierceCount (-1 = infinite) + hitCooldownPerEnemyMs (0 = no per-enemy cooldown, M5 Bolt baseline); projectile behavior gains homingTurnRate (rad/s, 0 = straight-line); Bolt def updated to read targeting:'nearest'/homingTurnRate:0 + per-level pierceCount:1/hitCooldownPerEnemyMs:0 — observable behavior preserved. messages.ts: FireEvent gains weaponLevel (lifts the M5 deferral comment about per-level visual scaling) + lockedTargetId (-1 = no lock; for Gakkung homing in US-003). rules.ts: Projectile struct gains lockedTargetId/homingTurnRate/pierceRemaining/hitCooldownPerEnemyMs/enemyHitCooldownsMs (server-only Map — landmine #1: NOT in schema); ProjectileContext gains serverNowMs (matches WeaponContext + OrbitHitCooldown wallclock convention); NEW exported selectTarget(state, player, targeting, rangeSq) handles all three modes (extracted so 'furthest' and 'facing' can be tested without polluting WEAPON_KINDS — Gakkung/Ahlspiess belong to US-003/004); NEW module-private rotateDirTowardInPlace() does 3D slerp with anti-parallel + within-step degenerate guards; tickWeapons projectile arm collapses to selectTarget() + emit (FireEvent now carries weaponLevel + lockedTargetId); tickProjectiles applies homing rotation BEFORE integration (homingTurnRate * dt cap, snap-on-close, anti-parallel safe), then multi-hit pass: collect all swept-sphere intersections up to pierceRemaining (or unbounded for -1), apply per-enemy cooldown, deal damage, emit hit + enemy_died as before, decrement pierceRemaining only for finite-pierce, drop projectile when budget exhausted. GameRoom.projectileCtx gains serverNowMs: () => Date.now(). Tests: makeProjectile + makeProjCtx helpers extended with new defaults so M4–M7 tests stay green untouched (Bolt-shape baseline); two M6-kills tests had inline-constructed ProjectileContext — added serverNowMs there too; integration.test.ts FirePayload type extended with weaponLevel + lockedTargetId, encoder regression now asserts both. NEW vitest cases (15): selectTarget × 6 (nearest/furthest/facing variants + range gating + 3D unit-length dir + null-when-no-targets), FireEvent payload × 1 (weaponLevel + lockedTargetId round-trip), tickProjectiles homing × 4 (within-step capped rotation, snap-on-small-angle, target-died-keep-heading, homingTurnRate=0 ignores lock), tickProjectiles pierce × 4 (single-hit baseline, pierce=2 multi-hit + decrement, infinite-pierce never decrements, hitCooldownPerEnemyMs gates same-enemy across ticks). NO name-based branching (rule 12 grep verified — no `name === "Bolt"`/`weaponName == ` anywhere in shared/server/client src). pnpm typecheck + 170 shared (was 155) / 34 client / 43 server tests pass — including integration encoder regression for the two new FireEvent fields.
