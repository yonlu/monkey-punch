{
  "name": "@mp/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@colyseus/schema": "^4.0.25",
    "alea": "^1.0.1",
    "simplex-noise": "^4.0.3"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
